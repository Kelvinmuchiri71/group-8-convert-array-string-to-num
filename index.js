// the array with strings 
const stringArray = ["1", "2", "3"];

//create a function which will convert it

function convertArray(strings) {
    return parseInt(strings);
}
const numArray = stringArray.map(convertArray); 



